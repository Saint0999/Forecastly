// OpenWeatherMap key. This is a client-side app, so the key is visible in the
// shipped bundle no matter how it's supplied -- it's kept here for simplicity.
// Free tier only; rotate it in the OWM dashboard if the quota gets abused.
export const API_KEY = "b165829b6d626fcd82a77fceb66003ee";
